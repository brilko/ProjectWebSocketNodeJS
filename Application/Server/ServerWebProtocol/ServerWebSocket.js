/**
 * Служит для того, чтобы отправлять клиентам строку по WebSocket со значениями датчика или "none".
 * 
 * Предоставляет функционал
 * Send(string) - отправляет строку клиенту по веб протоколу.
 * 
 * Не использует другие модули приложения.
 */

module.exports.Send(string) = function(string) { }