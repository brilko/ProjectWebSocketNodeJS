/**
 * (Б.3)Отправка обработанных значений. send(processedValues)
 * Ожидание: Каждую секунду сервер отправляет клиентам набор обработанных значений.
 * send(processedValues); // Функция отправки значений исполняется каждую секунду.
 */